- clean: ['~']

- link:
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.aliases: aliases
    ~/.vimrc: vimrc
    ~/.config/starship.toml: dotconf/config/starship.toml

- create:
    ~/.ssh:
        mode: 700
